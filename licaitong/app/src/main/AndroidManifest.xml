<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zsfinancialmanage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".other.OutManageActivity"></activity>
        <activity android:name=".other.InManageActivity" />
        <activity android:name=".activity.SysSettingActivity" />
        <activity android:name=".activity.DataAnalyseActivity" />
        <activity android:name=".activity.PayDetailActivity" />
        <activity android:name=".activity.NewPayActivity" />
        <activity android:name=".activity.InComeDetailActivity" />
        <activity android:name=".activity.NewInComeActivity" />
        <activity android:name=".view.RegisterActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.MainActivity" >

        </activity>
    </application>

</manifest>